$('#my-items').append('<%= j render @feed_items %>');
<% if @feed_items.next_page %>
  $('.pagination').replaceWith('<%= j paginate @feed_items %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
